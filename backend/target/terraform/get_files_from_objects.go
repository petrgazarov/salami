package terraform

import "salami/common/types"

func GetFilesFromObjects([]*types.Object) []*types.TargetFile {
	// All resources, variables and outputs follow the same file/folder structure as the source files.

	return nil
}
