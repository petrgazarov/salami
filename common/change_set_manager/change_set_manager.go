package change_set_manager

import (
	"salami/common/symbol_table"
	"salami/common/types"
)

func GenerateChangeSet(symbolTable *symbol_table.SymbolTable) (*types.ChangeSet, error) {
	return &types.ChangeSet{}, nil
}
