@resource(aws.s3.Bucket, CumuliAssumedRolesBucket)
Bucket: cumuli-assumed-roles
Versioning enabled

@resource(aws.s3.BucketPublicAccessBlock, CumuliAssetsPublicAccessBlock)
For $CumuliAssumedRolesBucket
Block public ACLs: True
Block public policy: False
Ignore public ACLs: True
Restrict public buckets: False

@resource(aws.s3.BucketPolicy, CumuliAssumedRolesBucketPolicy)
For $CumuliAssumedRolesBucket
Policy allows all principals to perform the "s3:GetObject" action on all objects in the bucket.